<!-- MainWindow.xaml -->
<Window x:Class="Contacts.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Contacts" Height="330" Width="520">
        <Window.Resources>
        <!-- стили / ресурсы -->
 <Style TargetType="TextBox">
        <!-- общий отступ для всех TextBox -->
        <Setter Property="Margin" Value="0,0,0,6"/>

        <!-- если в TextBox есть ошибка, показываем её в ToolTip -->
        <Style.Triggers>
            <Trigger Property="Validation.HasError" Value="True">
                <Setter Property="ToolTip"
                        Value="{Binding RelativeSource={RelativeSource Self},
                                        Path=(Validation.Errors)[0].ErrorContent}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <GroupBox Header="Edit Contact" Grid.Column="0" Margin="3">
        
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="3">
                    <TextBlock Text="Name:" Margin="0,0,0,3"/>
                    <TextBox Margin="0,0,0,6"
                            Text="{Binding Name, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, NotifyOnValidationError=True}" />
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="3">
                    <TextBlock Text="Phone Number:" Margin="0,0,0,3"/>
                    <TextBox Margin="0,0,0,6"
                             Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, NotifyOnValidationError=True }" />
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="3">
                    <TextBlock Text="Email:" Margin="0,0,0,3"/>
                    <TextBox Margin="0,0,0,6"
                             Text="{Binding Email, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, NotifyOnValidationError=True}" />
                </StackPanel>
            </Grid>
        </GroupBox>

        <GroupBox Header="Read Contact" Grid.Column="1" Margin="3">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="3">
                    <TextBlock Text="Name:"/>
                    <TextBlock FontWeight="Bold" Text="{Binding Name}" />
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="3">
                    <TextBlock Text="Phone Number:"/>
                    <TextBlock FontWeight="Bold" Text="{Binding PhoneNumber}" />
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="3">
                    <TextBlock Text="Email:"/>
                    <TextBlock FontWeight="Bold" Text="{Binding Email}" />
                </StackPanel>

                <StackPanel Grid.Row="4" Orientation="Horizontal"
                            HorizontalAlignment="Right" Margin="3">
                    <Button Width="75" Height="25" Margin="3"
                            Content="Save" Command="{Binding SaveCommand}" />
                    <Button Width="75" Height="25" Margin="3"
                            Content="Load" Command="{Binding LoadCommand}" />
                </StackPanel>
            </Grid>
        </GroupBox>
    </Grid>
</Window>